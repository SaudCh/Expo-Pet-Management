import React, { useState, useEffect } from 'react';
import { View, Text, TouchableOpacity, Platform, TextInput, Image, ScrollView, ScrollViewBase } from 'react-native'
import { SafeAreaView } from 'react-native-safe-area-context';
function Main({ navigation }) {
    return (
        <ScrollView>
            <SafeAreaView style={{ flex: 1 }}>
                <View style={{ flex: 1 }}>
                    
                </View>
            </SafeAreaView>
        </ScrollView>

    )
}

export default Main;
